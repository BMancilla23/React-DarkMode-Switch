## App - Dark Mode - React

---

Clnar Repositorio
```cmd
git clone <repositorio> <renombrar ? optional>
```

Instalar paquetes
```cmd
npm install
```

Iniciar servidor
```cmd
npm run dev --open
```

